package com.infovsn.homework;

import android.graphics.Color;

/**
 * Centralized colors used across activities for consistent styling.
 * Keep this small helper so we can change the green shade in one place later.
 */
public final class Colors {
    private Colors() {}

    // Exact green used in LCM screens (matches res/layout/lcm_division.xml #2E7D32)
    public static final int LCM_GREEN = 0xFF2E7D32;
    public static final int RED = 0xFFF44336;
}
