<?xml version="1.0" encoding="utf-8"?>
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="@color/menuButtonRipple">
    <item>
        <shape android:shape="rectangle">
            <gradient
                android:startColor="@color/menuButtonBgStart"
                android:endColor="@color/menuButtonBgEnd"
                android:angle="270" />
            <corners android:radius="16dp" />
            <stroke android:width="1dp" android:color="@color/menuButtonStroke" />
        </shape>
    </item>
</ripple>
